@model object
<h1>Resultados</h1>
@for (int i = 0; i < 10; i++) {
    <div class="row">
        <div class="col-md-2">
            Concepto @i.ToString()
        </div>
        <div class="col-md-9">
            Definición @i.ToString()
        </div>
        <div class="col-md-1">
            <a href="#" id="notlike">no Ok</a> / 
            <a href="#" id="like">Ok</a>
        </div>
    </div>
}